import { SortNumberPipePipe } from './sort-number-pipe.pipe';

describe('SortNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortNumberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
